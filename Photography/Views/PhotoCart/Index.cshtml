@model IEnumerable<Photography.PHOTO_CART>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Photo
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Photo.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Photo.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <img src="~/Photos/@Html.DisplayFor(modelItem => item.Photo.PhotoName)" />
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Photo.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Photo.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new {  id = item.PhotoId})
        </td>
    </tr>
}

</table>

<div class="form-group">
    <div class="col-md-offset-2 col-md-10">
        <a class="btn btn-default" href="Shipping/Create">Check out &raquo;</a>
    </div>
</div>